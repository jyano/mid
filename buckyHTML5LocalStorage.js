BUC = BUCKY = function () {
	$CSS(bucCSS)
	$.scI('leftbox', [
		$.f([
			$.p(['(key) One: ', $.ip().id('one')]),
			$.p(['(value) Two: ', $.ta().id('two')]),
			$.bt('save').id('bt')
		])
	])
	$.scI('rightbox', ['nothing yet hoss!'])
}
